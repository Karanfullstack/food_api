let JWT_SECRET,jwt;_f1e‍.x([["default",()=>_f1e‍.o]]);_f1e‍.w("../config/index",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_f1e‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
  static sign(payload, expiry = "120s", secret = JWT_SECRET) {
    return jwt.sign(payload, secret, {expiresIn: expiry});
  }
  static verify(token, secret = JWT_SECRET) {
    return jwt.verify(token, secret);
  }
}

_f1e‍.d(JwtService);
