let DEBUG_MODE,ValidationError;_76a‍.x([["default",()=>_76a‍.o]]);_76a‍.w("../config",[["DEBUG_MODE",["DEBUG_MODE"],function(v){DEBUG_MODE=v}]]);_76a‍.w("joi",[["ValidationError",["ValidationError"],function(v){ValidationError=v}]]);

const errorHandler = (err, req, res, next) => {
  let statusCode = 500;
  let data = {
    message: "Internal sever error",
    ...(DEBUG_MODE === "true" && {orignalError: err.message}),
  };

  if (err instanceof ValidationError) {
    statusCode = 422;
     data = {
      message: err.message,
    };
  }
  return res.status(statusCode).json(data);
};

_76a‍.d(errorHandler);
