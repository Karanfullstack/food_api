let Joi,CustomErrorHandler,User,bcrypt,JwtService;_52e‍.x([["default",()=>_52e‍.o]]);_52e‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_52e‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_52e‍.w("../../models",[["User",["User"],function(v){User=v}]]);_52e‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_52e‍.w("../../services/JwtSertice",[["default",["JwtService"],function(v){JwtService=v}]]);





const registerController = {
  async register(req, res, next) {
    // register controller function..
    // Validation Schema
    const registerSchema = Joi.object({
      name: Joi.string().min(3).max(30).required(),
      email: Joi.string().email().required(),
      password: Joi.string()
        .pattern(new RegExp("^[a-zA-Z0-9]{3,30}$"))
        .required(),
      repeat_password: Joi.ref("password"),
    });
    // End of Validation Schema

    const {error} = registerSchema.validate(req.body);
    if (error) {
      return next(error);
    }

    // Check if user is exists
    try {
      const exist = await User.exists({email: req.body.email});
      if (exist) {
        return next(CustomErrorHandler.alreadyExist());
      }
    } catch (err) {
      return next(err);
    }

    // Hash password...
    const {name, email, password} = req.body;
    const hashedPassword = await bcrypt.hash(password, 10);

    // prepare the model..
    const user = new User({
      name,
      email,
      password: hashedPassword,
    });

    // token variable
    let access_token;
    try {
      const result = await user.save();
      // JsonToken
      access_token = JwtService.sign({_id: result._id, role: result.role});
    } catch (error) {
      return next(error);
    }

    res.send({
      message: "Ok",
      access_token: access_token,
    });
  },
};

_52e‍.d(registerController);
