let Joi,CustomErrorHandler,User,bcrypt,JwtService;_3eb‍.x([["default",()=>_3eb‍.o]]);_3eb‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_3eb‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_3eb‍.w("../../models",[["User",["User"],function(v){User=v}]]);_3eb‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_3eb‍.w("../../services/JwtSertice",[["default",["JwtService"],function(v){JwtService=v}]]);





const registerController = {
  async register(req, res, next) {
    // register controller function..
    // Validation Schema
    const registerSchema = Joi.object({
      name: Joi.string().min(3).max(30).required(),
      email: Joi.string().email().required(),
      password: Joi.string()
        .pattern(new RegExp("^[a-zA-Z0-9]{3,30}$"))
        .required(),
      repeat_password: Joi.ref("password"),
    });
    // End of Validation Schema

    const {error} = registerSchema.validate(req.body);
    if (error) {
      return next(error);
    }

    // Check if user is exists
    try {
      const exist = await User.exists({email: req.body.email});
      if (exist) {
        return next(CustomErrorHandler.alreadyExist());
      }
    } catch (err) {
      return next(err);
    }

    // Hash password...
    const {name, email, password} = req.body;
    const hashedPassword = await bcrypt.hash(password, 10);

    // prepare the model..
    const user = new User({
      name,
      email,
      password: hashedPassword,
    });

    // token variable
    let access_token;
    try {
      const result = await user.save();
      // JsonToken
      access_token = JwtService.sign({_id: result._id, role: result.role});
    } catch (error) {
      return next(error);
    }

    res.send({
      message: "Ok",
      access_token: access_token,
    });
  },
};

_3eb‍.d(registerController);
