let JWT_SECRET,jwt;_b00‍.x([["default",()=>_b00‍.o]]);_b00‍.w("../config/index",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_b00‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
  static sign(payload, expiry = "60s", secret = JWT_SECRET) {
    return jwt.sign(payload, secret, {expiresIn: expiry});
  }
  static verify(token, secret = JWT_SECRET) {
    return jwt.verify(token, secret);
  }
} 

_b00‍.d(JwtService);
